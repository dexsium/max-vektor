name: iOS unsigned IPA

# Собирает Max Vektor на macOS-раннере GitHub и отдаёт НЕПОДПИСАННЫЙ .ipa
# артефактом. Подпись выполняется владельцем сборки самостоятельно —
# своим сертификатом (Xcode, AltStore, Sideloadly). Секреты, сертификаты
# и provisioning profile здесь не используются и не нужны.

on:
  workflow_dispatch:
  push:
    branches: [max-vektor-ios]

jobs:
  checks:
    name: analyze + test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - run: flutter pub get
      - run: flutter analyze
      - run: flutter test

  ipa:
    name: build unsigned .ipa
    needs: checks
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: flutter pub get
        run: flutter pub get

      # Podfile читает ios/Flutter/Generated.xcconfig (там FLUTTER_ROOT).
      # Этот файл создаёт НЕ `pub get`, а конфигурационный шаг сборки —
      # без него `pod install` падает сразу.
      # Номер сборки = номер запуска workflow: каждая сборка получает
      # свой CFBundleVersion, и по нему видно, что именно стоит на телефоне.
      # Это НАШ номер (Max Vektor). Версию официального клиента MAX, которой
      # приложение представляется серверу, он не трогает — она живёт в
      # MaxProto.appVersion/appBuild.
      - name: generate iOS config
        run: >-
          flutter build ios --release --no-codesign --config-only
          --build-number=${{ github.run_number }}

      - name: pod install
        working-directory: ios
        run: pod install --repo-update

      - name: build (no codesign)
        run: >-
          flutter build ios --release --no-codesign
          --build-number=${{ github.run_number }}

      - name: package Payload -> .ipa
        run: |
          set -euo pipefail
          APP="build/ios/iphoneos/Runner.app"
          test -d "$APP" || { echo "::error::$APP не собран"; exit 1; }
          rm -rf build/ipa && mkdir -p build/ipa/Payload
          cp -R "$APP" build/ipa/Payload/
          cd build/ipa
          IPA="max-vektor-b${{ github.run_number }}-unsigned.ipa"
          zip -qry "$IPA" Payload
          ls -lh "$IPA"

      - uses: actions/upload-artifact@v4
        with:
          name: max-vektor-unsigned-ipa
          path: build/ipa/max-vektor-b${{ github.run_number }}-unsigned.ipa
          if-no-files-found: error
          retention-days: 30
